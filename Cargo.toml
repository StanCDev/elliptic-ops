[package]
name = "elliptic_ops"
version = "0.1.0"
edition = "2024"

[dependencies]
ff = { version = "0.13.1", features = ["derive"] }
num-bigint = "0.4"
rand = "0.8"
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "curve_benchmarks"
harness = false